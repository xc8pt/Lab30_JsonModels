# Лабораторная работа №30. JSON и модели данных
## Информация о студенте
**ФИО:** Абдулин Ринат Рушанович
**Группа:** ИСП-233
**Дата:** 04.05.26
## Цели работы:
 - Разобраться с форматом JSON: структура, типы данных, вложенные объекты;
 - Научились настраивать формат JSON-ответов в ASP.NET Core;
 - Применили атрибуты [JsonPropertyName] и [JsonIgnore];
 - Поняли как enum сериализуется в JSON и как это изменить;
 - Увидели разницу между форматом по умолчанию и настроенным.
---
## Таблица всех маршрутов
| Атрибут / настройка               | Что делает                                | Пример                           |
|-----------------------------------|-------------------------------------------|----------------------------------|
| `[JsonPropertyName("name")]`      | Задаёт имя поля в JSON                    | `"name"` вместо `"Name"`         |
| `[JsonIgnore]`                    | Поле не попадает в JSON                   | Пароли, внутренние поля          |
| `PropertyNamingPolicy.CamelCase`  | Все поля в camelCase                      | `"realName"` вместо `"RealName"` |
| `JsonStringEnumConverter`         | Enum как строка                           | `"Marvel"` вместо `0`            |
| `WriteIndented = true`            | JSON с отступами                          | Для удобства при разработке      |
---
## Главные выводы:
 - **Что такое сериализация и десериализация?**
*Сериализация: Объект C# -> JSON-строка / Десериализация: JSON-строка -> объект C#*
 - **Зачем нужен [JsonIgnore]?**
*[JsonIgnore]: Исключить поле из JSON*
 - **Почему enum лучше сериализовывать строкой а не числом?**
*Чтобы избежать неоднозначности при добавлении / удалении значений enum и сохранить читаемость JSON.*
